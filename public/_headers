# Cloudflare Workers static-asset headers.
# /assets/* filenames are content-hashed by the build, so they are safe to
# cache forever; a new build changes the URL, never the content behind it.
/assets/*
  Cache-Control: public, max-age=31536000, immutable
